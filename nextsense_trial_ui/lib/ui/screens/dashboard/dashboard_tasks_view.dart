import 'package:flutter/material.dart';

class DashboardTasksView extends StatelessWidget {
  const DashboardTasksView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Tasks"));
  }
}
